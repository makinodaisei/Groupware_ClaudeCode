# Runtime dependencies for all Lambda functions
# boto3 is provided by the Lambda runtime; listed here for local dev only
boto3>=1.34.0
botocore>=1.34.0

# Test / dev dependencies
pytest>=8.0.0
pytest-cov>=5.0.0
moto[dynamodb,s3,cognitoidp]>=5.0.0
ruff>=0.4.0

# Documentation tools
openpyxl>=3.1.0
pyyaml>=6.0.0
